{
  "name": "scrupeak-blockchain",
  "version": "1.0.0",
  "scripts": {
    "test": "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.29.0"
  },
  "devDependencies": {
    "@types/mocha": "^9.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
